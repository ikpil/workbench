package org.example.atomic52.localfunctions

class Session(val title: String, val speeker: String)

val sessions = listOf(Session("Kotlin Coroutines", "Roman Elizarov"))

val favoriteSpeakers = setOf("Roman Elizarov")